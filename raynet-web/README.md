# raynet web

This is the experimental beta of the raynet browser client. It showcases on-device registration, peer discovery via BroadcastChannel, and one-to-one messaging.

## Development

```bash
npm install
npm run dev
```

Run type checking and build:

```bash
npx tsc --noEmit
npm run build
```
